appId: com.stopscrolling.desktop
productName: stopscrolling
directories:
  output: dist
  buildResources: build
files:
  - out/**/*
  - package.json
asar: true
mac:
  category: public.app-category.productivity
  target:
    - dmg
    - zip
  hardenedRuntime: true
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    CFBundleDisplayName: StopScrolling
    NSAppleEventsUsageDescription: StopScrolling reads the frontmost app and browser tab so it can record screen time.
win:
  target:
    - nsis
linux:
  category: Utility
  target:
    - AppImage
    - deb
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
